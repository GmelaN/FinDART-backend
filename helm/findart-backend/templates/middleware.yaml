{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "findart-backend.fullname" . }}-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /findart
{{- end }}