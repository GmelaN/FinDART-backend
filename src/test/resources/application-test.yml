spring:
    jpa:
        hibernate:
            ddl-auto: validate
        open-in-view: false
    flyway:
        enabled: true
        locations: classpath:db/migration

    # URL, user, and password are provided by the Testcontainers service connection.
    datasource:
        hikari:
            maximum-pool-size: 3
            minimum-idle: 1
